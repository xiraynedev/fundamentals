import {FC} from 'react';

export const Substring: FC = () => {

  const paragraph = 'Learning React with typescript.';

  console.log(paragraph.substring(0, 14));

  return (
    <div>

    </div>
  );
};
