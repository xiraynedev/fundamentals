import {FC} from 'react';

export const StartsWith: FC = () => {

  const paragraph = 'Learning React with typescript.';

  console.log(paragraph.startsWith('Learn'));

  return (
    <div>

    </div>
  );
};
