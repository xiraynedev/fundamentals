import {FC} from 'react';

export const Replace: FC = () => {

  const paragraph = 'Learning React with typescript.';

  console.log(paragraph.replace('typescript', 'TypeScript'));

  return (
    <div>

    </div>
  );
};
